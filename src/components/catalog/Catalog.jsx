import Filter from "./filter";

const Catalog = ({device}) => {

    return(
        <div>
            <Filter />
        </div>
    );
  
    // return (device === "web") ? (
    //     <div>
    //         <Filter />
    //     </div>
    // )
    // :
    // (
    //     <div>
            
    //     </div>
    // )

};

export default Catalog;
